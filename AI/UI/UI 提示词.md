在V0网站：帮我还原这几个页面！

找kimi：

```plain
你是前端架构师 +设计系统工程师。目标从现有项目的多个页面中抽取可复用组件，实现DRY，并构建一套 设计系统(Design Tokens + 组件规泡：+ 预览页面），统一主题色、样式与设计风格。要求：
1. 将所有组件封装到/components/ui/目录下，每个组件有单独的文件夹
2. DRY原则，消除重复样式与重复片段
3. 所有的组件再/playground下可查看，全部罗列出来
```