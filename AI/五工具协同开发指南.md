# 五工具协同开发指南

> **工具组合**：OpenSpec + Superpowers + Skills + CCG-Workflow + Vibe Kanban

---

## 目录

1. [工具全景](#1-工具全景)
2. [架构设计](#2-架构设计)
3. [各工具详解](#3-各工具详解)
4. [协同工作流](#4-协同工作流)
5. [完整演示](#5-完整演示用户仪表盘功能)
6. [决策指南](#6-决策指南)
7. [常见问题](#7-常见问题)

---

## 1. 工具全景

| 工具               | 核心定位   | 触发方式             | 关键能力                  |
| ---------------- | ------ | ---------------- | --------------------- |
| **OpenSpec**     | 规格驱动开发 | `/openspec:*` 命令 | 需求对齐、规格版本化、变更追踪       |
| **Superpowers**  | 开发流程管控 | 自动触发             | TDD、子代理驱动、系统化调试       |
| **Skills**       | 多模型协作  | 手动调用             | 多轮对话、交叉验证、灵活编排        |
| **CCG-Workflow** | 快速代码实现 | `/ccg:*` 命令      | 预设角色、自动路由、标准流程        |
| **Vibe Kanban**  | 任务隔离管理 | UI 操作            | Worktree 隔离、可视化、PR 管理 |

### 工具定位一句话

```
OpenSpec    → 定义「做什么」（规格对齐）
Superpowers → 管控「怎么做」（流程强制）
Skills      → 提供「多视角」（多模型协作）
CCG         → 加速「快速做」（预设角色）
Kanban      → 确保「隔离做」（任务管理）
```

---

## 2. 架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          完整开发流程                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────┐   ┌───────────┐   ┌───────────┐   ┌───────────┐         │
│  │ OpenSpec  │ → │Superpowers│ → │  Skills   │ → │    CCG    │         │
│  │ (规格对齐) │   │ (流程管控) │   │ (多模型)  │   │ (快速实现) │         │
│  └─────┬─────┘   └─────┬─────┘   └─────┬─────┘   └─────┬─────┘         │
│        │               │               │               │                │
│        └───────────────┴───────────────┴───────────────┘                │
│                               │                                         │
│                        ┌──────▼──────┐                                  │
│                        │ Vibe Kanban │                                  │
│                        │  (任务隔离)  │                                  │
│                        └─────────────┘                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流向

```
需求 → OpenSpec(规格) → Superpowers(计划) → Kanban(任务)
                              ↓
                    Skills(多模型验证)
                              ↓
                    CCG/Superpowers(实现)
                              ↓
                    Skills(多模型审查)
                              ↓
                    Kanban(合并) → OpenSpec(归档)
```

---

## 3. 各工具详解

### 3.1 OpenSpec - 规格驱动开发

**核心理念**：在写任何代码前，人和 AI 先对齐需求规格。

**目录结构**：
```
openspec/
├── specs/              # 当前系统规格（真相来源）
│   ├── auth.md
│   └── dashboard.md
├── changes/            # 提案变更
│   └── user-dashboard/
│       ├── proposal.md     # 功能提案
│       ├── tasks.md        # 任务清单
│       ├── design.md       # 设计文档
│       └── spec-delta.md   # 规格变更
└── archive/            # 已完成的变更
```

**关键命令**：
```bash
openspec init                    # 初始化项目
openspec list                    # 查看活跃变更
openspec show <change>           # 查看变更详情
openspec validate <change>       # 验证格式
openspec archive <change> --yes  # 归档变更
```

**Spec Delta 格式**：
```markdown
## ADDED Requirements
- REQ-001: 用户可以查看每日活动统计
  - Scenario: 用户访问仪表盘，系统显示今日登录次数

## MODIFIED Requirements
- REQ-002: 更新通知显示逻辑
  - Before: 显示所有通知
  - After: 仅显示未读通知，支持标记已读

## REMOVED Requirements
- REQ-003: 移除旧版统计页面
```

---

### 3.2 Superpowers - 开发流程管控

**核心理念**：强制执行最佳实践，不是建议而是必须。

**Skills 列表**：

| Skill                            | 触发场景     | 作用                    |
| -------------------------------- | -------- | --------------------- |
| `brainstorming`                  | 创建功能/组件前 | 想法 → 设计               |
| `writing-plans`                  | 有设计/需求后  | 设计 → 详细计划             |
| `executing-plans`                | 有计划后     | 批量执行 + 检查点            |
| `subagent-driven-development`    | 有计划后     | 子代理逐任务执行              |
| `test-driven-development`        | 实现代码时    | 强制 RED-GREEN-REFACTOR |
| `systematic-debugging`           | 遇到 bug 时 | 4 阶段根因分析              |
| `requesting-code-review`         | 任务完成后    | 请求审查                  |
| `receiving-code-review`          | 收到反馈后    | 处理反馈                  |
| `using-git-worktrees`            | 开始功能开发时  | 创建隔离分支                |
| `finishing-a-development-branch` | 所有任务完成后  | Merge/PR 决策           |
| `verification-before-completion` | 声称完成前    | 验证确实修复                |

**工作流程**：
```
brainstorming
     ↓
using-git-worktrees
     ↓
writing-plans
     ↓
subagent-driven-development / executing-plans
     ↓ (每个任务)
test-driven-development
     ↓
requesting-code-review
     ↓
finishing-a-development-branch
```

---

### 3.3 Skills - 多模型协作

**核心理念**：利用 Gemini 和 Codex 的不同专长，交叉验证。

**可用脚本**：
```bash
# Gemini Bridge (前端专家)
python ~/.claude/skills/collaborating-with-gemini/scripts/gemini_bridge.py \
  --cd "/project" \
  --PROMPT "分析前端架构..." \
  --SESSION_ID "xxx"  # 可选，多轮对话

# Codex Bridge (后端专家)
python ~/.claude/skills/collaborating-with-codex/scripts/codex_bridge.py \
  --cd "/project" \
  --sandbox read-only \
  --PROMPT "分析后端逻辑..." \
  --SESSION_ID "xxx"  # 可选，多轮对话
```

**关键参数**：

| 参数 | 说明 |
|------|------|
| `--cd` | 工作目录 |
| `--PROMPT` | 发送给模型的指令 |
| `--SESSION_ID` | 多轮对话 ID |
| `--sandbox` | Codex 沙盒策略：`read-only` / `workspace-write` / `danger-full-access` |
| `--return-all-messages` | 返回完整消息（含推理过程） |

**输出格式**：
```json
{
  "success": true,
  "SESSION_ID": "uuid-xxx",
  "agent_messages": "模型回复内容..."
}
```

---

### 3.4 CCG-Workflow - 快速代码实现

**核心理念**：预设专家角色，零 token 注入，标准化流程。

**关键命令**：

| 命令              | 用途          |
| --------------- | ----------- |
| `/ccg:workflow` | 完整 6 阶段开发流程 |
| `/ccg:feat`     | 功能开发 + 自动规划 |
| `/ccg:frontend` | 前端快速模式      |
| `/ccg:backend`  | 后端快速模式      |
| `/ccg:analyze`  | 代码分析        |
| `/ccg:debug`    | 调试模式        |
| `/ccg:optimize` | 性能优化        |
| `/ccg:review`   | 代码审查        |
| `/ccg:test`     | 测试生成        |

**专家角色**（12 个）：

| Codex 角色          | Gemini 角色          |
| ----------------- | ------------------ |
| Backend Architect | Frontend Architect |
| Backend Analyzer  | Frontend Analyzer  |
| Backend Debugger  | Frontend Debugger  |
| Backend Optimizer | Frontend Optimizer |
| Backend Reviewer  | Frontend Reviewer  |
| Backend Tester    | Frontend Tester    |

**核心约束**：
- 外部模型只返回 Unified Diff Patch
- Claude 持有所有写入权限
- 角色通过 `codeagent-wrapper` 零 token 注入

---

### 3.5 Vibe Kanban - 任务隔离管理

**核心理念**：每个任务在独立 worktree 中执行，互不干扰。

**任务状态流转**：
```
┌─────────────┬─────────────────┬─────────────┬────────┐
│   To Do     │   In Progress   │  In Review  │  Done  │
├─────────────┼─────────────────┼─────────────┼────────┤
│ 任务创建    │ Agent 执行中    │ 代码审查中   │ 已合并 │
│             │ worktree-xxx/   │ diff 查看   │        │
└─────────────┴─────────────────┴─────────────┴────────┘
```

**关键功能**：
- **Worktree 隔离**：每个任务独立分支和目录
- **实时日志**：查看 Agent 执行过程
- **内嵌预览**：启动 Dev Server 预览效果
- **Diff 审查**：逐行查看变更，添加评论
- **PR 管理**：创建 PR、Rebase、合并

**项目配置**：
```yaml
Setup Scripts: pnpm install     # 任务启动前
Dev Server: pnpm dev            # 预览服务
Cleanup Scripts: pnpm lint      # 任务完成后
Copy Files: .env, .env.local    # 复制到 worktree
```

---

## 4. 协同工作流

### 4.1 六阶段流程

```
┌──────────────────────────────────────────────────────────────────┐
│ Phase 0: 需求对齐                                                │
│ 工具: OpenSpec                                                   │
│ 产出: 规格文档 (openspec/changes/xxx/spec-delta.md)              │
├──────────────────────────────────────────────────────────────────┤
│ Phase 1: 深度设计                                                │
│ 工具: Superpowers (brainstorming) + Skills (多模型验证)          │
│ 产出: 设计文档 (docs/plans/xxx-design.md)                        │
├──────────────────────────────────────────────────────────────────┤
│ Phase 2: 实施计划                                                │
│ 工具: Superpowers (writing-plans)                                │
│ 产出: 详细计划 (docs/plans/xxx-implementation.md)                │
├──────────────────────────────────────────────────────────────────┤
│ Phase 3: 任务创建                                                │
│ 工具: Vibe Kanban                                                │
│ 产出: 隔离的 worktree 任务                                       │
├──────────────────────────────────────────────────────────────────┤
│ Phase 4: 代码实现                                                │
│ 工具: CCG-Workflow / Superpowers (subagent-driven)               │
│ 产出: 符合规格的代码                                             │
├──────────────────────────────────────────────────────────────────┤
│ Phase 5: 深度审查                                                │
│ 工具: Skills (多模型审阅) + Superpowers (code-review)            │
│ 产出: 审查通过的代码                                             │
├──────────────────────────────────────────────────────────────────┤
│ Phase 6: 合并归档                                                │
│ 工具: Vibe Kanban (PR) + OpenSpec (archive)                      │
│ 产出: 合并的代码 + 更新的规格                                    │
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 工具触发时机

| 场景           | 触发工具                 | 说明              |
| ------------ | -------------------- | --------------- |
| 新需求到来        | OpenSpec             | 先定义规格           |
| 需求不明确        | Superpowers + Skills | 头脑风暴 + 多模型讨论    |
| 开始实现前        | Superpowers          | 写详细计划           |
| 创建开发任务       | Kanban               | 隔离 worktree     |
| 快速实现（方案明确）   | CCG                  | 预设角色快速执行        |
| 复杂实现（需要 TDD） | Superpowers          | 子代理驱动 + TDD     |
| 需要多视角验证      | Skills               | 调用 Gemini/Codex |
| 代码审查         | Superpowers + Skills | 流程审查 + 多模型审查    |
| 合并代码         | Kanban               | PR 管理           |
| 完成功能         | OpenSpec             | 归档规格            |

---

## 5. 完整演示：用户仪表盘功能

### 5.0 场景描述

**需求**：为 Web 应用添加用户仪表盘功能
- 显示用户活动统计（日/周/月）
- 显示最近操作历史（最近 20 条）
- 系统通知面板

---

### 5.1 Phase 0: 需求对齐 (OpenSpec)

**操作**：
```bash
# 创建功能提案
/openspec:draft "用户仪表盘功能"
```

**OpenSpec 生成**：

```
openspec/changes/user-dashboard/
├── proposal.md
├── tasks.md
└── spec-delta.md
```

**proposal.md**：
```markdown
# 用户仪表盘功能提案

## 目标
为用户提供一个集中查看个人活动和系统通知的界面。

## 范围
- 活动统计展示
- 操作历史记录
- 通知管理

## 约束
- 遵循现有设计系统
- 响应式布局
- 性能：首屏加载 < 2s

## 成功标准
- 用户可以切换日/周/月视图
- 历史记录支持分页
- 通知支持标记已读
```

**spec-delta.md**：
```markdown
## ADDED Requirements

### REQ-DASH-001: 活动统计组件
用户可以查看活动统计数据
- Scenario: 用户访问仪表盘
  - Given: 用户已登录
  - When: 访问 /dashboard
  - Then: 显示今日/本周/本月活动统计

### REQ-DASH-002: 操作历史组件
用户可以查看最近操作记录
- Scenario: 查看操作历史
  - Given: 用户在仪表盘页面
  - When: 查看历史区域
  - Then: 显示最近 20 条操作，按时间倒序

### REQ-DASH-003: 通知面板
用户可以管理系统通知
- Scenario: 标记通知已读
  - Given: 存在未读通知
  - When: 点击通知
  - Then: 通知标记为已读，未读计数减少
```

**确认规格**：
```
你: "规格看起来完整吗？"
AI: "规格已定义，包含 3 个核心需求。确认后进入设计阶段。"
你: "确认，进入设计阶段"
```

---

### 5.2 Phase 1: 深度设计 (Superpowers + Skills)

**Superpowers 自动触发 brainstorming**：

```
AI: "我正在使用 brainstorming skill 来细化设计。"

AI: "首先，关于组件架构，你倾向于哪种方式？
    A) 单一 Dashboard 组件包含所有功能
    B) 拆分为独立 Widget 组件（推荐）
    C) 使用微前端架构"

你: "B"

AI: "好的。关于状态管理，你的项目目前使用什么方案？
    A) Redux
    B) Zustand
    C) React Query
    D) 没有全局状态管理"

你: "C，我们用 React Query"
```

**调用 Skills 获取多模型视角**：

```bash
# Claude 并行调用两个模型

# Gemini - 前端视角
python ~/.claude/skills/collaborating-with-gemini/scripts/gemini_bridge.py \
  --cd "/Users/oxy/my-project" \
  --PROMPT "We are designing a User Dashboard feature. Requirements:
- Activity statistics (daily/weekly/monthly)
- Recent operations history (last 20)
- Notification panel

From FRONTEND perspective, analyze:
1. Component architecture
2. State management with React Query
3. Data fetching patterns
4. Performance optimization

OUTPUT: Analysis and recommendations ONLY. No code."
```

**Gemini 返回**：
```json
{
  "success": true,
  "SESSION_ID": "gemini-session-001",
  "agent_messages": "## Frontend Analysis

### 1. Component Architecture
Recommend compositional approach:
- DashboardLayout (grid container)
- ActivityStatsWidget
  - TimeRangeSelector
  - StatCard (reusable)
  - ActivityChart
- RecentHistoryWidget
  - HistoryList
  - HistoryItem
- NotificationPanel
  - NotificationBadge
  - NotificationList
  - AlertItem

### 2. State Management
- Server State: React Query for all API data
- Local State: useState for UI interactions (selected range)
- Avoid global state except notification count

### 3. Data Fetching
- Parallel fetching for independent widgets
- Window focus refetching for freshness
- SSE/WebSocket for real-time notifications

### 4. Performance
- Virtualize long lists
- Memoize expensive chart calculations
- Code split dashboard route"
}
```

```bash
# Codex - 后端视角
python ~/.claude/skills/collaborating-with-codex/scripts/codex_bridge.py \
  --cd "/Users/oxy/my-project" \
  --sandbox read-only \
  --PROMPT "We are designing a User Dashboard feature. Requirements:
- Activity statistics (daily/weekly/monthly)
- Recent operations history (last 20)
- Notification panel

From BACKEND perspective, analyze:
1. API endpoint design
2. Database schema
3. Caching strategy
4. Security considerations

OUTPUT: Analysis and recommendations ONLY. No code."
```

**Codex 返回**：
```json
{
  "success": true,
  "SESSION_ID": "codex-session-001",
  "agent_messages": "## Backend Analysis

### 1. API Design
- GET /v1/users/me/activity-stats?range=daily|weekly|monthly
- GET /v1/users/me/operations?limit=20&cursor=xxx
- GET /v1/users/me/notifications?status=unread|all
- PATCH /v1/users/me/notifications/mark-read (bulk)

### 2. Database Schema
- user_activity_events (raw events)
- user_activity_rollups (pre-aggregated stats)
- notifications + user_notifications (separate content/state)

### 3. Caching
- Redis for rollup stats (5min TTL)
- ETag for operations list
- Cache unread_count separately

### 4. Security
- Use /me endpoint to prevent IDOR
- Rate limiting on dashboard endpoints
- Validate range parameters"
}
```

**交叉审阅**：
```bash
# Gemini 审阅后端方案
python ~/.claude/skills/collaborating-with-gemini/scripts/gemini_bridge.py \
  --cd "/Users/oxy/my-project" \
  --SESSION_ID "gemini-session-001" \
  --PROMPT "Review this backend API design from frontend perspective:
- GET /v1/users/me/activity-stats?range=daily|weekly|monthly
- GET /v1/users/me/operations?limit=20&cursor=xxx

Issues or suggestions?"
```

**Gemini 审阅意见**：
```
1. Stats endpoint should include comparison data (delta vs previous period)
2. Operations should include human-readable 'summary' field
3. Need lightweight /notifications/count endpoint for badge
4. Stats response should be array format for charts: [{date, value}]
```

**设计文档产出**：

```markdown
# docs/plans/2024-01-15-user-dashboard-design.md

# 用户仪表盘设计文档

## 架构概览

### 前端组件
- DashboardLayout
- ActivityStatsWidget (TimeRangeSelector, StatCard, ActivityChart)
- RecentHistoryWidget (HistoryList, HistoryItem)
- NotificationPanel (NotificationBadge, NotificationList, AlertItem)

### 后端 API
- GET /v1/users/me/activity-stats?range=...
- GET /v1/users/me/operations?limit=20&cursor=...
- GET /v1/users/me/notifications?status=...
- PATCH /v1/users/me/notifications/mark-read

### 技术决策
- 前端: React + TypeScript + React Query + Recharts + Tailwind
- 后端: 现有框架 + Redis 缓存
- 实时: SSE for notifications

### 经过验证的改进
- Stats 包含 delta_percentage 对比数据
- Operations 包含 summary 字段
- 增加 /notifications/count 轻量端点
```

---

### 5.3 Phase 2: 实施计划 (Superpowers)

**Superpowers 自动触发 writing-plans**：

```
AI: "我正在使用 writing-plans skill 创建实施计划。"
```

**生成计划**：

```markdown
# docs/plans/2024-01-15-user-dashboard-implementation.md

# User Dashboard Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans

**Goal:** Implement user dashboard with activity stats, history, and notifications

**Architecture:** Widget-based composition with React Query for data fetching

**Tech Stack:** React, TypeScript, React Query, Recharts, Tailwind CSS

---

## Frontend Tasks

### Task 1: Create Dashboard Route and Layout

**Files:**
- Create: `src/pages/Dashboard.tsx`
- Create: `src/components/Dashboard/DashboardLayout.tsx`
- Modify: `src/App.tsx` (add route)

**Step 1: Write failing test**
```typescript
// tests/pages/Dashboard.test.tsx
describe('Dashboard', () => {
  it('renders dashboard layout', () => {
    render(<Dashboard />);
    expect(screen.getByTestId('dashboard-layout')).toBeInTheDocument();
  });
});
```

**Step 2: Run test to verify failure**
```bash
pnpm test tests/pages/Dashboard.test.tsx
# Expected: FAIL - Cannot find module
```

**Step 3: Implement minimal code**
```typescript
// src/pages/Dashboard.tsx
export default function Dashboard() {
  return <DashboardLayout data-testid="dashboard-layout" />;
}
```

**Step 4: Run test to verify pass**
```bash
pnpm test tests/pages/Dashboard.test.tsx
# Expected: PASS
```

**Step 5: Commit**
```bash
git add src/pages/Dashboard.tsx src/components/Dashboard/
git commit -m "feat(dashboard): add dashboard route and layout"
```

---

### Task 2: Implement ActivityStatsWidget

**Files:**
- Create: `src/components/Dashboard/ActivityStats/ActivityStatsWidget.tsx`
- Create: `src/components/Dashboard/ActivityStats/StatCard.tsx`
- Create: `src/hooks/useActivityStats.ts`

**Step 1: Write failing test**
```typescript
describe('ActivityStatsWidget', () => {
  it('displays stats for selected range', async () => {
    render(<ActivityStatsWidget />);
    await waitFor(() => {
      expect(screen.getByText(/logins/i)).toBeInTheDocument();
    });
  });
});
```

[... 继续 TDD 步骤 ...]

---

### Task 3: Implement RecentHistoryWidget
[详细 TDD 步骤...]

### Task 4: Implement NotificationPanel
[详细 TDD 步骤...]

---

## Backend Tasks

### Task 5: Create Activity Stats Endpoint
[详细 TDD 步骤...]

### Task 6: Create Operations History Endpoint
[详细 TDD 步骤...]

### Task 7: Create Notifications Endpoints
[详细 TDD 步骤...]

---

## Integration Tasks

### Task 8: Connect Frontend to Backend
[详细步骤...]

### Task 9: Add E2E Tests
[详细步骤...]
```

---

### 5.4 Phase 3: 任务创建 (Vibe Kanban)

**在 Kanban 创建任务**：

```
点击 [+] 创建任务

任务 1: [Frontend] Dashboard UI
├── 描述: 实现 Task 1-4（组件和 hooks）
├── Agent: Claude Code
├── 命令提示: 参考 docs/plans/2024-01-15-user-dashboard-implementation.md
├── Base Branch: main
└── 自动创建 worktree: worktree-frontend-dashboard/

任务 2: [Backend] Dashboard API
├── 描述: 实现 Task 5-7（API 端点）
├── Agent: Claude Code
├── 命令提示: 参考 docs/plans/2024-01-15-user-dashboard-implementation.md
├── Base Branch: main
└── 自动创建 worktree: worktree-backend-dashboard/
```

**看板状态**：
```
┌─────────────────┬─────────────────┬─────────────┬────────┐
│     To Do       │   In Progress   │  In Review  │  Done  │
├─────────────────┼─────────────────┼─────────────┼────────┤
│ [Frontend]      │                 │             │        │
│ Dashboard UI    │                 │             │        │
│ worktree-fe/    │                 │             │        │
├─────────────────┤                 │             │        │
│ [Backend]       │                 │             │        │
│ Dashboard API   │                 │             │        │
│ worktree-be/    │                 │             │        │
└─────────────────┴─────────────────┴─────────────┴────────┘
```

---

### 5.5 Phase 4: 代码实现 (CCG + Superpowers)

**启动前端任务**：

在 Kanban 点击 [Frontend] 任务 → Start

**方式 A: 使用 CCG 快速实现**

```bash
# 在 worktree-frontend-dashboard/ 中
/ccg:frontend "实现用户仪表盘前端组件，参考：
- 规格: openspec/changes/user-dashboard/spec-delta.md
- 计划: docs/plans/2024-01-15-user-dashboard-implementation.md
- Tasks 1-4

要求：
1. 遵循 TDD
2. 使用 React Query
3. 输出 Unified Diff Patch"
```

**方式 B: 使用 Superpowers 子代理驱动**

```
AI: "我正在使用 subagent-driven-development skill。"

对每个 Task:
1. 启动子代理执行
2. 规格合规审查
3. 代码质量审查
4. 通过后继续下一个
```

**并行启动后端任务**：

```bash
# 在 worktree-backend-dashboard/ 中
/ccg:backend "实现用户仪表盘后端 API，参考：
- 规格: openspec/changes/user-dashboard/spec-delta.md
- 计划: docs/plans/2024-01-15-user-dashboard-implementation.md
- Tasks 5-7

要求：
1. 遵循 TDD
2. 包含缓存策略
3. 输出 Unified Diff Patch"
```

**看板状态更新**：
```
┌─────────────┬─────────────────────┬─────────────┬────────┐
│   To Do     │    In Progress      │  In Review  │  Done  │
├─────────────┼─────────────────────┼─────────────┼────────┤
│             │ [Frontend] 🟢       │             │        │
│             │   Implementing...   │             │        │
│             │   Task 2/4          │             │        │
│             ├─────────────────────┤             │        │
│             │ [Backend] 🟢        │             │        │
│             │   Implementing...   │             │        │
│             │   Task 1/3          │             │        │
└─────────────┴─────────────────────┴─────────────┴────────┘
```

---

### 5.6 Phase 5: 深度审查 (Skills + Superpowers)

**任务完成，进入 Review**：

```
┌─────────────┬─────────────────┬───────────────────┬────────┐
│   To Do     │   In Progress   │     In Review     │  Done  │
├─────────────┼─────────────────┼───────────────────┼────────┤
│             │                 │ [Frontend] ⏳     │        │
│             │                 │   +12 files       │        │
│             │                 │   +850 lines      │        │
│             │                 ├───────────────────┤        │
│             │                 │ [Backend] ⏳      │        │
│             │                 │   +8 files        │        │
│             │                 │   +620 lines      │        │
└─────────────┴─────────────────┴───────────────────┴────────┘
```

**Superpowers 触发 requesting-code-review**：

```
AI: "我正在使用 requesting-code-review skill。

检查清单：
✅ 所有测试通过
✅ 符合计划中的架构
✅ 无明显代码异味
⚠️ 发现 1 个中等问题：NotificationPanel 缺少加载状态"
```

**调用 Skills 多模型深度审查**：

```bash
# Gemini 审阅前端
python ~/.claude/skills/collaborating-with-gemini/scripts/gemini_bridge.py \
  --cd "/Users/oxy/my-project" \
  --SESSION_ID "gemini-session-001" \
  --PROMPT "Review this frontend implementation against the spec:

SPEC: [openspec/changes/user-dashboard/spec-delta.md 内容]

DIFF: [前端 diff 内容]

Check:
1. Component structure matches design
2. React Query usage is correct
3. Accessibility (a11y) compliance
4. Performance patterns

Output specific issues with file:line references."
```

**Gemini 审阅结果**：
```
## Frontend Review

### Issues Found

1. **[Medium]** `ActivityStatsWidget.tsx:45`
   - Missing loading skeleton, shows blank during fetch
   - Fix: Add Suspense boundary or loading state

2. **[Low]** `StatCard.tsx:23`
   - Delta percentage missing aria-label for screen readers
   - Fix: Add aria-label={`Change: ${delta}%`}

3. **[Suggestion]** `useActivityStats.ts:12`
   - staleTime could be longer (5min → 10min) for stats that don't change often

### Approved Patterns
✅ Component composition is clean
✅ React Query hooks well structured
✅ Chart memoization correctly implemented
```

```bash
# Codex 审阅后端
python ~/.claude/skills/collaborating-with-codex/scripts/codex_bridge.py \
  --cd "/Users/oxy/my-project" \
  --sandbox read-only \
  --SESSION_ID "codex-session-001" \
  --PROMPT "Review this backend implementation against the spec:

SPEC: [openspec/changes/user-dashboard/spec-delta.md 内容]

DIFF: [后端 diff 内容]

Check:
1. API contract matches spec
2. Security (IDOR, injection, rate limiting)
3. Error handling edge cases
4. Cache invalidation correctness

Output specific issues with file:line references."
```

**Codex 审阅结果**：
```
## Backend Review

### Issues Found

1. **[High]** `NotificationController.ts:67`
   - Bulk mark-read doesn't validate notification ownership
   - Risk: User could mark other users' notifications as read
   - Fix: Add WHERE user_id = current_user check

2. **[Medium]** `ActivityStatsService.ts:34`
   - No cap on date range parameter
   - Risk: Could request 10 years of data, expensive query
   - Fix: Add max 90 days validation

3. **[Low]** `OperationsRepository.ts:89`
   - Missing index hint comment for cursor query
   - Suggestion: Add comment explaining index usage

### Approved Patterns
✅ /me endpoint prevents IDOR on main queries
✅ Redis caching correctly implemented
✅ ETag handling is correct
```

**修复问题**：

```
AI: "发现以下问题需要修复：

Frontend:
- NotificationPanel 添加加载状态
- StatCard 添加 aria-label

Backend:
- 修复 bulk mark-read 权限检查
- 添加日期范围验证

正在修复..."
```

**再次审查 → 通过**

---

### 5.7 Phase 6: 合并归档 (Kanban + OpenSpec)

**Kanban 合并**：

```
1. [Backend] → Create PR
   Title: feat(dashboard): add dashboard API endpoints
   → Review → Approve → Merge to main ✓

2. [Frontend] → Rebase onto main (获取后端更新)
   → 解决冲突（如有）
   → Create PR
   Title: feat(dashboard): add dashboard UI components
   → Review → Approve → Merge to main ✓
```

**看板最终状态**：
```
┌─────────────┬─────────────────┬─────────────┬──────────────────┐
│   To Do     │   In Progress   │  In Review  │      Done        │
├─────────────┼─────────────────┼─────────────┼──────────────────┤
│             │                 │             │ [Frontend] ✅    │
│             │                 │             │   Merged         │
│             │                 │             ├──────────────────┤
│             │                 │             │ [Backend] ✅     │
│             │                 │             │   Merged         │
└─────────────┴─────────────────┴─────────────┴──────────────────┘
```

**OpenSpec 归档**：

```bash
openspec archive user-dashboard --yes

# 执行：
# 1. spec-delta.md 合并到 specs/dashboard.md
# 2. changes/user-dashboard/ 移动到 archive/
# 3. 规格成为新的真相来源
```

**最终 specs/dashboard.md**：
```markdown
# Dashboard Specifications

## REQ-DASH-001: Activity Statistics
User can view activity statistics with daily/weekly/monthly granularity.
[完整规格...]

## REQ-DASH-002: Operation History
User can view recent 20 operations with pagination support.
[完整规格...]

## REQ-DASH-003: Notification Panel
User can manage system notifications with mark-read capability.
[完整规格...]
```

---

## 6. 决策指南

### 6.1 工具选择流程图

```
                    ┌─────────────────┐
                    │   新需求到来    │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   OpenSpec      │
                    │   定义规格      │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  需要深度设计？  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
         是   ▼                        否   ▼
    ┌─────────────────┐           ┌─────────────────┐
    │   Superpowers   │           │   直接进入      │
    │   brainstorming │           │   Kanban 创建   │
    │       +         │           └────────┬────────┘
    │    Skills       │                    │
    └────────┬────────┘                    │
             │                              │
    ┌────────▼────────┐                    │
    │   Superpowers   │◀───────────────────┘
    │   writing-plans │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │  Vibe Kanban    │
    │  创建隔离任务    │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │   实现方式？     │
    └────────┬────────┘
             │
    ┌────────┴────────────────┐
    │                         │
快速 ▼                   标准 ▼
┌─────────────┐       ┌─────────────────┐
│ CCG-Workflow│       │   Superpowers   │
│ /ccg:feat   │       │ subagent-driven │
└──────┬──────┘       └────────┬────────┘
       │                       │
       └───────────┬───────────┘
                   │
          ┌────────▼────────┐
          │  需要深度审查？  │
          └────────┬────────┘
                   │
          ┌────────┴────────┐
          │                 │
     是   ▼            否   ▼
┌─────────────┐    ┌─────────────┐
│   Skills    │    │ Superpowers │
│ 多模型审阅  │    │ code-review │
└──────┬──────┘    └──────┬──────┘
       │                  │
       └────────┬─────────┘
                │
       ┌────────▼────────┐
       │  Kanban 合并    │
       │  OpenSpec 归档  │
       └─────────────────┘
```

### 6.2 快速参考表

| 我想要... | 使用工具 |
|----------|---------|
| 定义清晰的需求规格 | OpenSpec |
| 探索设计方案 | Superpowers (brainstorming) |
| 获取多模型意见 | Skills |
| 写详细实施计划 | Superpowers (writing-plans) |
| 创建隔离的开发任务 | Vibe Kanban |
| 快速实现（方案明确） | CCG-Workflow |
| TDD 驱动实现 | Superpowers (subagent-driven) |
| 多模型交叉审查 | Skills |
| 标准代码审查 | Superpowers (code-review) |
| 合并代码和 PR | Vibe Kanban |
| 归档完成的规格 | OpenSpec |

---

## 7. 常见问题

### Q1: 所有工具都必须用吗？

**不是**。根据任务复杂度选择：

| 任务类型 | 推荐工具组合 |
|---------|-------------|
| 简单 bug 修复 | Superpowers only |
| 小功能添加 | Superpowers + Kanban |
| 中等功能 | OpenSpec + Superpowers + Kanban |
| 复杂功能 | 全部五个工具 |
| 多模型验证需求 | 加上 Skills |
| 快速原型 | CCG + Kanban |

### Q2: 工具之间会冲突吗？

**不会**。每个工具有独立的触发方式：
- OpenSpec: `/openspec:*` 命令
- Superpowers: 自动触发
- Skills: 手动调用 Python 脚本
- CCG: `/ccg:*` 命令
- Kanban: UI 操作

### Q3: 如何处理紧急修复？

可以跳过部分阶段：
```
紧急修复流程:
1. 直接在 Kanban 创建任务
2. 使用 CCG 快速实现
3. Superpowers 自动触发 TDD
4. 快速审查后合并
5. 事后补充 OpenSpec 规格
```

### Q4: Skills 的 SESSION_ID 什么时候用？

当需要**多轮对话**时使用：
- 第一次调用：获取 SESSION_ID
- 后续调用：传入 SESSION_ID 保持上下文
- 适合：设计迭代、深度审阅、问题追问

### Q5: CCG 和 Superpowers 选哪个实现？

| 选择 CCG | 选择 Superpowers |
|---------|-----------------|
| 方案已经很明确 | 需要 TDD 强制执行 |
| 想要预设专家角色 | 需要子代理逐任务审查 |
| 快速原型 | 高质量生产代码 |
| 单一前端/后端任务 | 复杂多步骤任务 |

---

## 附录 A: 工具安装

```bash
# OpenSpec
npm install -g @fission-ai/openspec@latest
openspec init

# Superpowers (Claude Code)
/plugin marketplace add obra/superpowers-marketplace
/plugin install superpowers@superpowers-marketplace

# CCG-Workflow
npx ccg-workflow

# Vibe Kanban
npx vibe-kanban

# Skills (已内置)
# ~/.claude/skills/collaborating-with-gemini/
# ~/.claude/skills/collaborating-with-codex/
```

## 附录 B: 相关链接

- [OpenSpec GitHub](https://github.com/Fission-AI/OpenSpec)
- [Superpowers GitHub](https://github.com/obra/superpowers)
- [CCG-Workflow GitHub](https://github.com/fengshao1227/ccg-workflow)
- [Vibe Kanban GitHub](https://github.com/BloopAI/vibe-kanban)
- [Vibe Kanban Docs](https://www.vibekanban.com/docs)
