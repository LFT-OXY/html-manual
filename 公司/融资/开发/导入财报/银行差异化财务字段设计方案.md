### 📋 问题分析

| 当前状态               | 需要支持                         |
| ---------------------- | -------------------------------- |
| 统一存储 56 个财务字段 | 不同银行需要不同字段子集         |
| 固定展示所有字段       | 字段顺序、名称因银行而异         |
| 无计算字段支持         | 需要流动比率、速动比率等派生字段 |

### 🏗️ 推荐方案：配置驱动 + 模板导出

**核心思路**：数据存储统一，展示/导出通过配置差异化

```plain
┌─────────────────┐     ┌───────────────────┐     ┌─────────────────┐
│ financial_      │     │ bank_report_      │     │ 报告生成         │
│ extract_record  │────▶│ field_config      │────▶│ (Word/Excel)    │
│ (原始数据)       │     │ (银行字段配置)     │     │                 │
└─────────────────┘     └───────────────────┘     └─────────────────┘
```

### 📊 数据库设计

#### 1. 字段定义表（规范化字段）

```sql
CREATE TABLE financial_field_def (
  field_key      VARCHAR(50) PRIMARY KEY COMMENT '规范字段键: monetary_funds',
  field_name     VARCHAR(100) NOT NULL COMMENT '标准名称: 货币资金',
  category       VARCHAR(50) COMMENT '分类: current_assets/fixed_assets/...',
  default_unit   VARCHAR(20) DEFAULT '元' COMMENT '默认单位',
  aliases        JSON COMMENT '别名列表: ["现金及现金等价物", "银行存款"]',
  is_computed    TINYINT DEFAULT 0 COMMENT '是否为计算字段',
  formula        VARCHAR(500) COMMENT '计算公式: current_assets / current_liabilities'
);
```

#### 2. 银行报告配置表

```sql
CREATE TABLE bank_report_config (
  bank_code      VARCHAR(20) PRIMARY KEY COMMENT '银行代码: ADBC/ICBC/CCB',
  bank_name      VARCHAR(100) NOT NULL COMMENT '银行名称',
  version        INT DEFAULT 1 COMMENT '配置版本',
  update_time    DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. 银行字段配置表（核心）

```sql
CREATE TABLE bank_report_field (
  id             BIGINT PRIMARY KEY AUTO_INCREMENT,
  bank_code      VARCHAR(20) NOT NULL COMMENT '银行代码',
  field_key      VARCHAR(50) NOT NULL COMMENT '字段键',
  display_label  VARCHAR(100) COMMENT '银行特定显示名称（可为空，使用默认）',
  section        VARCHAR(50) COMMENT '所属区块: borrower_assets/guarantor_liabilities',
  display_order  INT DEFAULT 0 COMMENT '显示顺序',
  required       TINYINT DEFAULT 0 COMMENT '是否必填',
  decimals       INT DEFAULT 2 COMMENT '小数位数',
  role           VARCHAR(20) DEFAULT 'both' COMMENT '适用角色: borrower/guarantor/both',
  
  UNIQUE KEY uk_bank_field (bank_code, field_key, role)
);
```

### 📝 配置示例

```sql
-- 农发行配置
INSERT INTO bank_report_field (bank_code, field_key, display_label, section, display_order, role) VALUES
('ADBC', 'monetary_funds', '货币资金', 'current_assets', 1, 'both'),
('ADBC', 'accounts_receivable', '应收账款', 'current_assets', 2, 'borrower'),
('ADBC', 'inventory', '存货', 'current_assets', 3, 'borrower'),
('ADBC', 'total_assets', '资产总计', 'summary', 10, 'both'),
('ADBC', 'debt_ratio', '资产负债率', 'ratios', 20, 'both');

-- 工商银行配置（不同字段）
INSERT INTO bank_report_field (bank_code, field_key, display_label, section, display_order, role) VALUES
('ICBC', 'monetary_funds', '货币资金', 'assets', 1, 'both'),
('ICBC', 'short_term_loans', '短期借款', 'liabilities', 5, 'borrower'),
('ICBC', 'current_ratio', '流动比率', 'analysis', 15, 'both'),
('ICBC', 'quick_ratio', '速动比率', 'analysis', 16, 'both');
```

### 🔧 计算字段支持

```java
// 计算公式示例（存储在 financial_field_def.formula）
{
  "current_ratio": "current_assets / current_liabilities",
  "quick_ratio": "(current_assets - inventory) / current_liabilities",
  "debt_ratio": "total_liabilities / total_assets * 100"
}
```

### 🌐 API 设计

```plain
GET /api/banks/{bankCode}/financial-report
    ?companyId=123
    &role=borrower|guarantor
    &years=2023,2024

Response:
{
  "bankCode": "ADBC",
  "bankName": "农发行",
  "companyId": 123,
  "companyName": "XX公司",
  "role": "borrower",
  "sections": [
    {
      "name": "current_assets",
      "title": "流动资产",
      "fields": [
        {
          "fieldKey": "monetary_funds",
          "label": "货币资金",
          "values": { "2023": 1000000, "2024": 1200000 },
          "unit": "元",
          "isComputed": false
        },
        ...
      ]
    },
    {
      "name": "ratios",
      "title": "财务指标",
      "fields": [
        {
          "fieldKey": "current_ratio",
          "label": "流动比率",
          "values": { "2023": 1.5, "2024": 1.8 },
          "isComputed": true,
          "formula": "流动资产 / 流动负债"
        }
      ]
    }
  ],
  "missingFields": ["inventory"]  // 必填但缺失的字段
}
```

### 📈 架构优势

| 特性         | 说明                             |
| ------------ | -------------------------------- |
| **扩展性**   | 新增银行只需插入配置，无代码改动 |
| **灵活性**   | 同一数据可按银行不同展示         |
| **可审计**   | 配置版本化，变更可追溯           |
| **计算字段** | 公式化定义，自动计算             |
| **角色区分** | 借款人/担保人可显示不同字段      |

------

### 银行差异化财务字段方案详解

### 🎯 核心设计理念

**一句话总结**：数据存一份，展示按配置

```plain
┌────────────────────────────────────────────────────────────────────┐
│                          设计哲学                                   │
├────────────────────────────────────────────────────────────────────┤
│  存储层：统一规范，存所有字段（56个+）                                │
│  配置层：每个银行定义"我要哪些字段、叫什么名、什么顺序"                 │
│  展示层：根据配置动态渲染，同一份数据 → 不同银行的报表                  │
└────────────────────────────────────────────────────────────────────┘
```

------

### 📊 三层表结构详解

#### 第一层：字段定义表 `financial_field_def`

**作用**：定义系统支持的所有财务字段的"规范"

```sql
CREATE TABLE financial_field_def (
  field_key      VARCHAR(50) PRIMARY KEY,  -- 唯一标识，英文
  field_name     VARCHAR(100) NOT NULL,    -- 标准中文名
  category       VARCHAR(50),              -- 所属分类
  default_unit   VARCHAR(20) DEFAULT '元',
  aliases        JSON,                     -- 别名（用于 Excel/PDF 解析匹配）
  is_computed    TINYINT DEFAULT 0,        -- 是否计算字段
  formula        VARCHAR(500)              -- 计算公式
);
```

**数据示例**：

| field_key             | field_name   | category       | is_computed | formula                                              |
| --------------------- | ------------ | -------------- | ----------- | ---------------------------------------------------- |
| `monetary_funds`      | 货币资金     | current_assets | 0           | NULL                                                 |
| `accounts_receivable` | 应收账款     | current_assets | 0           | NULL                                                 |
| `inventory`           | 存货         | current_assets | 0           | NULL                                                 |
| `current_assets`      | 流动资产合计 | summary        | 0           | NULL                                                 |
| `current_liabilities` | 流动负债合计 | summary        | 0           | NULL                                                 |
| `current_ratio`       | 流动比率     | ratios         | **1**       | `current_assets / current_liabilities`               |
| `quick_ratio`         | 速动比率     | ratios         | **1**       | `(current_assets - inventory) / current_liabilities` |

**为什么需要这张表？**

1. **规范化**：所有银行、所有数据都用同一套 `field_key`
2. **别名匹配**：PDF 里写"现金及等价物"，Excel 里写"货币资金"，都能匹配到 `monetary_funds`
3. **计算字段定义**：流动比率等指标的公式集中管理

------

#### 第二层：原始数据表 `financial_extract_record`（已存在，小改）

**作用**：存储从 PDF/Excel 解析出的实际财务数据

```sql
-- 现有表，建议添加 field_key 字段
ALTER TABLE financial_extract_record 
  ADD COLUMN field_key VARCHAR(50) COMMENT '规范字段键，关联 financial_field_def';
```

**数据示例**（某企业 2023-2024 年数据）：

| company_id | field_key           | field_name   | fiscal_year | value    | original_unit |
| ---------- | ------------------- | ------------ | ----------- | -------- | ------------- |
| 1          | monetary_funds      | 货币资金     | 2023        | 5000000  | 元            |
| 1          | monetary_funds      | 货币资金     | 2024        | 6200000  | 元            |
| 1          | accounts_receivable | 应收账款     | 2023        | 3000000  | 元            |
| 1          | accounts_receivable | 应收账款     | 2024        | 3500000  | 元            |
| 1          | inventory           | 存货         | 2023        | 2000000  | 元            |
| 1          | current_assets      | 流动资产合计 | 2023        | 15000000 | 元            |
| 1          | current_liabilities | 流动负债合计 | 2023        | 10000000 | 元            |

**关键点**：

- 保留 `field_name`（原始名称，用于追溯）
- 新增 `field_key`（规范键，用于查询和配置匹配）
- 数据只存一份，不管给哪个银行用

------

#### 第三层：银行配置表 `bank_report_field`

**作用**：定义每个银行需要哪些字段、怎么显示

```sql
CREATE TABLE bank_report_field (
  id             BIGINT PRIMARY KEY AUTO_INCREMENT,
  bank_code      VARCHAR(20) NOT NULL,     -- 银行代码：ADBC/ICBC
  field_key      VARCHAR(50) NOT NULL,     -- 要哪个字段
  display_label  VARCHAR(100),             -- 银行特定的显示名称
  section        VARCHAR(50),              -- 分组：assets/liabilities/ratios
  display_order  INT DEFAULT 0,            -- 显示顺序
  required       TINYINT DEFAULT 0,        -- 是否必填
  role           VARCHAR(20) DEFAULT 'both' -- borrower/guarantor/both
);
```

**配置示例对比**：

 

**农发行 (ADBC) 配置**：

| bank_code | field_key           | display_label | section | display_order | role     |
| --------- | ------------------- | ------------- | ------- | ------------- | -------- |
| ADBC      | monetary_funds      | 货币资金      | assets  | 1             | both     |
| ADBC      | accounts_receivable | 应收账款      | assets  | 2             | borrower |
| ADBC      | inventory           | 存货          | assets  | 3             | borrower |
| ADBC      | total_assets        | 资产总计      | summary | 10            | both     |
| ADBC      | debt_ratio          | 资产负债率    | ratios  | 20            | both     |

**工商银行 (ICBC) 配置**（不同字段、不同顺序）：

| bank_code | field_key        | display_label    | section   | display_order | role     |
| --------- | ---------------- | ---------------- | --------- | ------------- | -------- |
| ICBC      | monetary_funds   | **现金及等价物** | liquidity | 1             | both     |
| ICBC      | short_term_loans | 短期借款         | debt      | 5             | borrower |
| ICBC      | long_term_loans  | 长期借款         | debt      | 6             | borrower |
| ICBC      | current_ratio    | 流动比率         | analysis  | 15            | both     |
| ICBC      | quick_ratio      | 速动比率         | analysis  | 16            | both     |

**配置的威力**：

- 农发行要 5 个字段，工行要 5 个字段，但**只有 1 个重合**（货币资金）
- 同样是货币资金，农发行显示"货币资金"，工行显示"现金及等价物"
- 无需改代码，只需改配置

------

### 🔄 数据流转过程

```plain
用户请求: 获取企业A在农发行报告中的财务数据

          ┌─────────────────────────────────────────────────────────┐
          │  Step 1: 加载银行配置                                    │
          │  SELECT * FROM bank_report_field WHERE bank_code='ADBC' │
          │  → 得到农发行需要的 5 个字段及其显示规则                    │
          └─────────────────────────────────────────────────────────┘
                                    ↓
          ┌─────────────────────────────────────────────────────────┐
          │  Step 2: 查询原始数据                                    │
          │  SELECT * FROM financial_extract_record                 │
          │  WHERE company_id = ? AND field_key IN (配置的字段列表)   │
          │  → 只查需要的字段，不查全部 56 个                          │
          └─────────────────────────────────────────────────────────┘
                                    ↓
          ┌─────────────────────────────────────────────────────────┐
          │  Step 3: 计算派生字段                                    │
          │  如果配置包含 current_ratio (流动比率)                    │
          │  → 用公式 current_assets / current_liabilities 计算      │
          └─────────────────────────────────────────────────────────┘
                                    ↓
          ┌─────────────────────────────────────────────────────────┐
          │  Step 4: 应用显示规则                                    │
          │  - 替换 display_label                                   │
          │  - 按 display_order 排序                                │
          │  - 按 section 分组                                      │
          └─────────────────────────────────────────────────────────┘
                                    ↓
          ┌─────────────────────────────────────────────────────────┐
          │  Step 5: 返回结构化数据                                  │
          │  {                                                      │
          │    "sections": [                                        │
          │      { "name": "assets", "fields": [...] },             │
          │      { "name": "ratios", "fields": [...] }              │
          │    ]                                                    │
          │  }                                                      │
          └─────────────────────────────────────────────────────────┘
```

------

### 💻 后端实现示例

```java
@Service
public class BankFinancialReportService {

    /**
     * 获取银行特定的财务报告
     */
    public BankFinancialReportVO getReport(String bankCode, Long companyId, 
                                           String role, List<Integer> years) {
        // 1. 加载银行配置
        List<BankReportField> configs = bankReportFieldMapper
            .selectByBankAndRole(bankCode, role);
        
        // 2. 提取需要的字段键
        Set<String> fieldKeys = configs.stream()
            .map(BankReportField::getFieldKey)
            .collect(toSet());
        
        // 3. 查询原始数据
        List<FinancialExtractRecord> records = financialRecordMapper
            .selectByCompanyAndFields(companyId, fieldKeys, years);
        
        // 4. 转换为 Map 便于计算
        // Map<fieldKey, Map<year, value>>
        Map<String, Map<Integer, BigDecimal>> dataMap = buildDataMap(records);
        
        // 5. 计算派生字段
        for (BankReportField config : configs) {
            FinancialFieldDef def = fieldDefMap.get(config.getFieldKey());
            if (def.isComputed()) {
                Map<Integer, BigDecimal> computed = calculateField(
                    def.getFormula(), dataMap, years);
                dataMap.put(config.getFieldKey(), computed);
            }
        }
        
        // 6. 组装返回结果
        return buildReportVO(bankCode, configs, dataMap, years);
    }
    
    /**
     * 计算派生字段
     * 公式示例: "current_assets / current_liabilities"
     */
    private Map<Integer, BigDecimal> calculateField(
            String formula, 
            Map<String, Map<Integer, BigDecimal>> dataMap,
            List<Integer> years) {
        
        Map<Integer, BigDecimal> result = new HashMap<>();
        for (Integer year : years) {
            BigDecimal value = FormulaEvaluator.evaluate(
                formula, 
                fieldKey -> dataMap.getOrDefault(fieldKey, Map.of())
                                   .get(year)
            );
            result.put(year, value);
        }
        return result;
    }
}
```

------

### 🖥️ 前端展示示例

```vue
<template>
  <div class="bank-financial-report">
    <!-- 银行选择 -->
    <el-select v-model="selectedBank" @change="loadReport">
      <el-option label="农发行" value="ADBC" />
      <el-option label="工商银行" value="ICBC" />
    </el-select>
    
    <!-- 按 section 分组显示 -->
    <div v-for="section in reportData.sections" :key="section.name" class="section">
      <h3>{{ section.title }}</h3>
      <table>
        <thead>
          <tr>
            <th>项目</th>
            <th v-for="year in years" :key="year">{{ year }}年</th>
          </tr>
        </thead>
        <tbody>
          <tr v-for="field in section.fields" :key="field.fieldKey">
            <td>{{ field.label }}</td>
            <td v-for="year in years" :key="year">
              {{ formatValue(field.values[year], field.isComputed) }}
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</template>
```

**农发行报告展示**：

| 项目         | 2023年 | 2024年 |
| ------------ | ------ | ------ |
| **资产**     |        |        |
| 货币资金     | 500万  | 620万  |
| 应收账款     | 300万  | 350万  |
| 存货         | 200万  | 180万  |
| **汇总**     |        |        |
| 资产总计     | 1500万 | 1700万 |
| **财务指标** |        |        |
| 资产负债率   | 45%    | 42%    |

**工商银行报告展示**（同一企业，不同展示）：

| 项目         | 2023年 | 2024年 |
| ------------ | ------ | ------ |
| **流动性**   |        |        |
| 现金及等价物 | 500万  | 620万  |
| **负债**     |        |        |
| 短期借款     | 200万  | 150万  |
| 长期借款     | 300万  | 280万  |
| **分析指标** |        |        |
| 流动比率     | 1.50   | 1.65   |
| 速动比率     | 1.30   | 1.47   |

------

### 🎨 可视化架构图

```plain
┌─────────────────────────────────────┐
                    │         用户界面 (Vue)               │
                    │  ┌─────────┐  ┌─────────┐           │
                    │  │ 农发行   │  │ 工商银行 │  ...      │
                    │  └────┬────┘  └────┬────┘           │
                    └───────┼────────────┼────────────────┘
                            │            │
                            ▼            ▼
        ┌───────────────────────────────────────────────────┐
        │              API 层                                │
        │  GET /api/banks/{bankCode}/financial-report       │
        └───────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────────────────┐
        │              Service 层                            │
        │  ┌─────────────────────────────────────────────┐  │
        │  │ 1. 读取银行配置 (bank_report_field)          │  │
        │  │ 2. 查询原始数据 (financial_extract_record)   │  │
        │  │ 3. 计算派生字段 (流动比率等)                  │  │
        │  │ 4. 应用显示规则 (标签、排序、分组)            │  │
        │  └─────────────────────────────────────────────┘  │
        └───────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────────────────┐
        │              数据库层                              │
        │                                                   │
        │   ┌─────────────────┐    ┌─────────────────────┐  │
        │   │ financial_      │    │ bank_report_field   │  │
        │   │ field_def       │    │ (银行配置)           │  │
        │   │ (字段定义)       │    │                     │  │
        │   └────────┬────────┘    └──────────┬──────────┘  │
        │            │                        │             │
        │            ▼                        │             │
        │   ┌─────────────────┐               │             │
        │   │ financial_      │◄──────────────┘             │
        │   │ extract_record  │  (通过 field_key 关联)      │
        │   │ (原始数据)       │                            │
        │   └─────────────────┘                             │
        └───────────────────────────────────────────────────┘
```

------

### ✅ 方案优势总结

| 优势           | 说明                           |
| -------------- | ------------------------------ |
| **零代码扩展** | 新增银行只需插入配置，不改代码 |
| **数据不冗余** | 财务数据只存一份，多银行共用   |
| **灵活映射**   | 同字段不同名称、不同顺序都支持 |
| **计算字段**   | 流动比率等自动计算，公式可配置 |
| **角色区分**   | 借款人/担保人可展示不同字段    |
| **易于维护**   | 银行需求变更只改配置表         |





农发行的财报结构更加完整和复杂，需要额外添加的字段：

 

**资产类（需要新增）：**

| 字段键                | 字段名         | 分类               |
| --------------------- | -------------- | ------------------ |
| `biological_assets`   | 生产性生物资产 | non_current_assets |
| `investment_property` | 投资性房地产   | non_current_assets |

**利润表相关（需要新增）：**

| 字段键                     | 字段名           | 分类   |
| -------------------------- | ---------------- | ------ |
| `operating_revenue`        | 营业收入         | income |
| `operating_cost`           | 营业成本         | income |
| `business_tax_surcharges`  | 营业税金及附加   | income |
| `sales_expenses`           | 销售费用         | income |
| `management_expenses`      | 管理费用         | income |
| `financial_expenses`       | 财务费用         | income |
| `asset_impairment_loss`    | 资产减值损失     | income |
| `fair_value_change_income` | 公允价值变动收益 | income |
| `investment_income`        | 投资收益         | income |
| `operating_profit`         | 营业利润         | income |
| `non_operating_income`     | 营业外收入       | income |
| `non_operating_expenses`   | 营业外支出       | income |
| `total_profit`             | 利润总额         | income |
| `income_tax_expense`       | 所得税费用       | income |
| `net_profit`               | 净利润           | income |

**现金流量表（需要新增）：**

| 字段键                           | 字段名                             | 分类      |
| -------------------------------- | ---------------------------------- | --------- |
| `cash_received_from_sales`       | 销售商品、提供劳务收到的现金       | cash_flow |
| `tax_refunds_received`           | 收到的税费返还                     | cash_flow |
| `other_cash_received_operating`  | 收到其他与经营活动有关的现金       | cash_flow |
| `operating_cash_inflow`          | 经营活动现金流入小计               | cash_flow |
| `cash_paid_for_goods`            | 购买商品、接受劳务支付的现金       | cash_flow |
| `cash_paid_to_employees`         | 支付给职工以及为职工支付的现金     | cash_flow |
| `taxes_paid`                     | 支付的各项税费                     | cash_flow |
| `other_cash_paid_operating`      | 支付其他与经营活动有关的现金       | cash_flow |
| `operating_cash_outflow`         | 经营活动现金流出小计               | cash_flow |
| `net_cash_flow_operating`        | 经营活动产生的现金流量净额         | cash_flow |
| `cash_received_from_investments` | 收回投资收到的现金                 | cash_flow |
| `investment_income_received`     | 取得投资收益收到的现金             | cash_flow |
| `cash_from_disposal_assets`      | 处置资产收回的现金净额             | cash_flow |
| `investing_cash_inflow`          | 投资活动现金流入小计               | cash_flow |
| `cash_paid_for_assets`           | 购建资产支付的现金                 | cash_flow |
| `cash_paid_for_investments`      | 投资支付的现金                     | cash_flow |
| `investing_cash_outflow`         | 投资活动现金流出小计               | cash_flow |
| `net_cash_flow_investing`        | 投资活动产生的现金流量净额         | cash_flow |
| `cash_received_from_investors`   | 吸收投资收到的现金                 | cash_flow |
| `cash_received_from_borrowings`  | 取得借款收到的现金                 | cash_flow |
| `financing_cash_inflow`          | 筹资活动现金流入小计               | cash_flow |
| `cash_paid_for_debt`             | 偿还债务支付的现金                 | cash_flow |
| `cash_paid_for_dividends`        | 分配股利、利润或偿付利息支付的现金 | cash_flow |
| `financing_cash_outflow`         | 筹资活动现金流出小计               | cash_flow |
| `net_cash_flow_financing`        | 筹资活动产生的现金流量净额         | cash_flow |
| `net_increase_cash`              | 现金及现金等价物净增加额           | cash_flow |
| `beginning_cash_balance`         | 期初现金及现金等价物余额           | cash_flow |
| `ending_cash_balance`            | 期末现金及现金等价物余额           | cash_flow |

**财务指标类（需要新增）：**

| 字段键                         | 字段名                    | 分类     |
| ------------------------------ | ------------------------- | -------- |
| `inventory_turnover`           | 存货周转率                | computed |
| `accounts_receivable_turnover` | 应收账款周转率            | computed |
| `current_asset_turnover`       | 流动资产周转率            | computed |
| `total_asset_turnover`         | 总资产周转率              | computed |
| `interest_coverage_ratio`      | 利息保障倍数/已获利息倍数 | computed |
| `cost_expense_profit_ratio`    | 成本费用利润率            | computed |
| `sales_profit_ratio`           | 销售（营业）利润率        | computed |
| `total_asset_return_ratio`     | 总资产报酬率              | computed |
| `return_on_equity`             | 净资产收益率              | computed |
| `sales_growth_rate`            | 销售（营业）增长率        | computed |
| `profit_growth_rate`           | 销售（营业）利润增长率    | computed |
| `total_asset_growth_rate`      | 总资产增长率              | computed |
| `capital_accumulation_rate`    | 资本积累率                | computed |
| `capitalization_ratio`         | 全部资本化比率            | computed |

**所有者权益扩展（需要新增）：**

| 字段键                       | 字段名                     | 分类   |
| ---------------------------- | -------------------------- | ------ |
| `other_equity_instruments`   | 其他权益工具               | equity |
| `other_comprehensive_income` | 其他综合收益               | equity |
| `minority_interest`          | 少数股东权益               | equity |
| `parent_equity`              | 归属于母公司所有者权益合计 | equity |